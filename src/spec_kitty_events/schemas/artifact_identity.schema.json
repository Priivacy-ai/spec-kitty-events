{
  "$id": "spec-kitty-events/artifact_identity",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Canonical identity for one artifact instance.\n\nartifact_class is the SINGLE source of truth for artifact taxonomy.\nEvent payloads MUST NOT duplicate this field at the top level.",
  "properties": {
    "artifact_class": {
      "enum": [
        "input",
        "workflow",
        "output",
        "evidence",
        "policy",
        "runtime"
      ],
      "title": "Artifact Class",
      "type": "string"
    },
    "mission_type": {
      "minLength": 1,
      "title": "Mission Type",
      "type": "string"
    },
    "path": {
      "description": "Repository-relative path",
      "minLength": 1,
      "title": "Path",
      "type": "string"
    },
    "run_id": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Run Id"
    },
    "wp_id": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Wp Id"
    }
  },
  "required": [
    "mission_type",
    "path",
    "artifact_class"
  ],
  "title": "ArtifactIdentity",
  "type": "object"
}
